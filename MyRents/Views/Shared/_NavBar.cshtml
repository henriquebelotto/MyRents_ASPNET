<!--Partial View for the Navigation Bar-->

<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
    <a class="navbar-brand" href="/">MyRents</a>
    <button class="navbar-toggler" aria-expanded="false" aria-controls="navbarColor01" aria-label="Toggle navigation" type="button" data-target="#navbarColor01" data-toggle="collapse">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-between" id="navbarColor01">
        <div>
            <ul class="navbar-nav  ml-3">
                <li class="nav-item no-underline @(ViewBag.Title == "Customers" ? "active" : "")">
                    @Html.ActionLink("Customers", "Index", "Customers")
                </li>
                <li class="nav-item no-underline @(ViewBag.Title == "Movies" ? "active" : "")">
                    @Html.ActionLink("Movie", "Index", "Movies")
                </li>
            </ul>
        </div>
        <div>
            @Html.Partial("_LoginPartial")
        </div>
        
    </div>
</nav>

